package Clases;

/*public sealed class SealedClass permits SealedClass2{ // Si la clase está en el mismo fichero
    // "permits Clases.SealedClass" se puede omitir



}
non-sealed class  SealedClass2 extends SealedClass{ // puede ser sealed , non-sealed o final


}
 sealed interface SealedInterface permits SealedInterface2{ // Las interfaces tb pueden ser sealed

}

non-sealed interface SealedInterface2 extends SealedInterface{ // las interfaces que heredan no pueden ser final
    //sólo non-sealed o sealed

} */